.scripts:
    report: &report_script |
        import pandas as pd
        from waluigi.sdk.context import context
        from waluigi.sdk.catalog import catalog
        from waluigi.catalog.models import SourceCreateRequest, SourceType

        date = context.params.date
        print(context)

        print(f"Building global report for {date} ...")

        catalog.create_source(SourceCreateRequest(
            id="analytics-local",
            type=SourceType.LOCAL,
            config={},
            description="Local storage for analytics pipeline",
        ))

        sources = ["erp", "web", "social"]
        frames  = []
        lineage = []

        for source in sources:
            reader = catalog.read_dataset(f"analytics/{source}/clean/clean_{source}")
            df     = reader.read()
            df["pipeline_source"] = source
            frames.append(df)
            lineage.append({"dataset_id": reader.dataset_id, "version": reader.version})
            print(f"  {source}: {len(df)} rows @ {reader.version}")

        report_df = pd.concat(frames, ignore_index=True)
        print(f"Total rows in report: {len(report_df)}")

        report_id = "analytics/reports/global_report"

        handle = catalog.create_dataset(
            report_id,
            format="parquet",
            source_id="analytics-local",
            description="Global consolidated report across all sources",
        )

        handle.set_chart("value_by_source", "Total value by source", spec={
            "type": "bar",
            "x":   {"field": "pipeline_source", "label": "Source"},
            "y":   {"field": "value", "agg": "sum", "label": "Total Value"},
        })
        handle.set_chart("value_by_metric", "Value by metric", spec={
            "type": "bar",
            "x":   {"field": "metric", "label": "Metric"},
            "y":   {"field": "value", "agg": "sum", "label": "Total Value"},
        })
        handle.set_chart("value_share_by_source", "Value share by source", spec={
            "type": "pie",
            "x":   {"field": "pipeline_source"},
            "y":   {"field": "value", "agg": "sum"},
        })
        handle.set_chart("value_distribution", "Value distribution", spec={
            "type": "histogram",
            "x":   {"field": "value", "label": "Value"},
            "bins": 10,
        })
        handle.set_chart("value_by_category", "Value by category", spec={
            "type": "bar",
            "x":   {"field": "category", "label": "Category"},
            "y":   {"field": "value", "agg": "sum", "label": "Total Value"},
        })

        with handle.create_version(metadata={"date": date}, inputs=lineage) as writer:
            writer.write(report_df)

        if writer.skipped:
            print(f"Skipped — same metadata, existing version: {writer.version}")
        else:
            print(f"Done: {writer.dataset_id} @ {writer.version} ({len(report_df)} rows)")
    
    clean: &clean_script |
        import random
        import pandas as pd
        from waluigi.sdk.context import context
        from waluigi.sdk.catalog import catalog
        from waluigi.catalog.models import SourceCreateRequest, SourceType

        source    = context.params.source.lower()
        date      = context.params.date
        fail_prob = float(context.attributes.fail_prob)

        if random.random() < fail_prob:
            raise RuntimeError(f"Simulated failure while cleaning {source}")

        catalog.create_source(SourceCreateRequest(
            id="analytics-local",
            type=SourceType.LOCAL,
            config={},
            description="Local storage for analytics pipeline",
        ))

        raw_id   = f"analytics/{source}/raw/raw_{source}"
        clean_id = f"analytics/{source}/clean/clean_{source}"

        reader = catalog.read_dataset(raw_id)
        df     = reader.read()
        print(f"Read {len(df)} rows from {raw_id} @ {reader.version}")

        df = df.dropna()
        df["metric"] = df["metric"].str.strip().str.lower()
        df["value"]  = df["value"].astype(float)
        print(f"After cleaning: {len(df)} rows")

        handle = catalog.create_dataset(
            clean_id,
            format="parquet",
            source_id="analytics-local",
            description=f"Cleaned data for {source}",
        )
        handle.set_expectations([
            {"rule_id": "expect_column_values_to_not_be_null",  "inputs": {"x": "this.metric"}, "tolerance": 1.0},
            {"rule_id": "expect_column_values_to_not_be_null",  "inputs": {"x": "this.value"},  "tolerance": 1.0},
            {"rule_id": "expect_column_values_to_be_unique",    "inputs": {"x": "this.metric"}, "tolerance": 1.0},
            {"rule_id": "expect_column_values_to_be_between",   "inputs": {"x": "this.value"},
             "params": {"min_val": 0, "max_val": 1_000_000}, "tolerance": 1.0},
            {"rule_id": "expect_column_values_to_be_of_type",   "inputs": {"x": "this.value"},
             "params": {"target_type": "float"}, "tolerance": 1.0},
        ])

        lineage = [{"dataset_id": reader.dataset_id, "version": reader.version}]
        with handle.create_version(metadata={"date": date, "source": source}, inputs=lineage) as writer:
            writer.write(df)

        if writer.skipped:
            print(f"Skipped — same metadata, existing version: {writer.version}")
        else:
            print(f"Done: {writer.dataset_id} @ {writer.version} ({len(df)} rows)")

    extract: &extract_script |
        import random
        import pandas as pd
        from waluigi.sdk.context import context
        from waluigi.sdk.catalog import catalog
        from waluigi.catalog.models import SourceCreateRequest, SourceType

        METRICS = {
            "erp":    [("revenue",     "finance"),     ("costs",       "finance"),
                       ("orders",      "operations"),  ("refunds",     "finance")],
            "web":    [("sessions",    "traffic"),     ("pageviews",   "traffic"),
                      ("conversions", "acquisition"), ("bounce_rate", "engagement")],
            "social": [("followers",   "audience"),    ("impressions", "reach"),
                       ("engagements", "interaction"), ("shares",      "viral")],
        }

        source = context.params.source.lower()
        date   = context.params.date

        catalog.create_source(SourceCreateRequest(
            id="analytics-local",
            type=SourceType.LOCAL,
            config={},
            description="Local storage for analytics pipeline",
        ))

        print(f"Extracting raw data for source: {source}, date: {date}")

        rows = [
            {"date": date, "source": source, "metric": metric,
             "value": round(random.uniform(100, 10_000), 2), "category": category}
            for metric, category in METRICS[source]
        ]
        df = pd.DataFrame(rows)

        handle = catalog.create_dataset(
            f"analytics/{source}/raw/raw_{source}",
            format="parquet",
            source_id="analytics-local",
            description=f"Raw extracted data for {source}",
        )

        with handle.create_version(metadata={"date": date, "source": source}) as writer:
            writer.write(df)

        if writer.skipped:
            print(f"Skipped — same metadata, existing version: {writer.version}")
        else:
            print(f"Done: {writer.dataset_id} @ {writer.version} ({len(df)} rows)")


kind: DAG
metadata:
    workdir: "work"
spec:
    name: "GlobalReport"
    id: "final_report"
    namespace: "analytics"
    script: *report_script
    params:
      date: "2026-05-15"
    attributes:
      var: "pippo"
    resources:
      coin: 1
    affinity:
      - python
    requires:
      - name: "CleanDataTask"
        namespace: "analytics"
        id: "clean_erp"
        script: *clean_script
        params:
          source: "ERP"
          date: "${parent.params.date}"
        attributes:
          fail_prob: "0.0"
        resources:
          pdc: 1
        affinity:
          - python
        requires:
          - name: "RawDataExtract"
            namespace: "analytics"
            id: "extract_erp"
            script: *extract_script
            params:
              source: "${parent.params.source}"
              date: "${parent.params.date}"
            resources:
              coin: 1
            affinity:
              - python
      - name: "CleanDataTask"
        namespace: "analytics"
        id: "clean_web"
        script: *clean_script
        params:
          source: "WEB"
          date: "${parent.params.date}"
        attributes:
          fail_prob: "0.0"
        resources:
          pdc: 1
        affinity:
          - python
        requires:
          - name: "RawDataExtract"
            namespace: "analytics"
            id: "extract_web"
            script: *extract_script
            params:
              source: "${parent.params.source}"
              date: "${parent.params.date}"
            resources:
              coin: 1
            affinity:
              - python
      - name: "CleanDataTask"
        namespace: "analytics"
        id: "clean_social"
        script: *clean_script
        params:
          source: "SOCIAL"
          date: "${parent.params.date}"
        attributes:
          fail_prob: "0.0"
        resources:
          pdc: 1
        affinity:
          - python
        requires:
          - name: "RawDataExtract"
            namespace: "analytics"
            id: "extract_social"
            script: *extract_script
            params:
              source: "${parent.params.source}"
              date: "${parent.params.date}"
            resources:
              coin: 1
            affinity:
              - python