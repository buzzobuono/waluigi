kind: DAG
metadata:
    workdir: "work"
spec:
    name: "CreateAnalyticsSource"
    id:   "catalog_create_source_test"
    namespace: "catalog"
    type: "CatalogCreateSource"
    config:
        id:          "test-local"
        type:        "LOCAL"
        description: "Local storage for the test dataset"
        config:
            base_path: "data"
    resources:
        coin: 1
    affinity:
        - python
