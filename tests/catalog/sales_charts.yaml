- key: revenue_by_category_bar
  title: Revenue by Category (bar)
  spec:
    type: bar
    x:
      field: category
      label: Category
    y:
      field: revenue
      agg: sum
      label: Total Revenue (€)

- key: revenue_over_time_line
  title: Revenue over Time grouped by Category (line)
  spec:
    type: line
    x:
      field: date
      label: Month
    y:
      field: revenue
      agg: sum
      label: Revenue (€)
    color: category

- key: revenue_share_pie
  title: Revenue share by Category (pie)
  spec:
    type: pie
    x:
      field: category
    y:
      field: revenue
      agg: sum

- key: units_histogram
  title: Units distribution (histogram)
  spec:
    type: histogram
    x:
      field: units
      label: Units Sold
    bins: 6

- key: revenue_vs_units_scatter
  title: Revenue vs Units (scatter)
  spec:
    type: scatter
    x:
      field: units
      label: Units Sold
    y:
      field: revenue
      label: Revenue (€)
    limit: 100

- key: category_radar
  title: Category performance radar (radar)
  spec:
    type: radar
    group_by: category
    axes:
      - field: revenue
        label: Revenue (€)
        max: 50000
      - field: units
        label: Units Sold
        max: 3500
      - field: returns
        label: Returns
        max: 200
    agg: sum
