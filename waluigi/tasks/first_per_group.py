"""
FirstPerGroup — keep the first row per group after sorting.

See last_per_group.py for full documentation.
"""
from waluigi.tasks.last_per_group import _run


def run():
    _run("first")


if __name__ == "__main__":
    run()
